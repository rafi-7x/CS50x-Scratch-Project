# CS50x-Scratch-Project
This project features two interactive sprites with distinct movement mechanics. The first sprite moves whenever the spacebar is pressed and changes its costume upon pressing the 'b' key. The second sprite is controlled via camera motion sensing, moving dynamically with user actions and changing its costume only when it touches the first sprite.
